{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Chrome debugger in VSCode",
            "url": "http://localhost:4200",
            "webRoot": "${workspaceFolder}"
        }
    ]
}